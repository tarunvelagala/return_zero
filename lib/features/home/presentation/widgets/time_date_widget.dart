import 'package:flutter/material.dart';

class TimeDateWidget extends StatelessWidget {
  const TimeDateWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('TimeDateWidget');
  }
}
